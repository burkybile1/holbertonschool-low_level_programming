#include <stdio.h>


/**
 * main - will print the alphabet in lowercase
 * followed by a new line.
 *
 * Return: Always (Success)
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);

	printf("\n");

	return (0);
}
